<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xgn.cms.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.xgn.cms.domain.User">
    <id column="userId" jdbcType="INTEGER" property="userId" />
    <result column="userName" jdbcType="VARCHAR" property="userName" />
    <result column="priority" jdbcType="BIGINT" property="priority" />
    <result column="password" jdbcType="VARCHAR" property="password" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from user
    where userId = #{userId,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.xgn.cms.domain.User">
    insert into user (userId, userName, priority, 
      password)
    values (#{userId,jdbcType=INTEGER}, #{userName,jdbcType=VARCHAR}, #{priority,jdbcType=BIGINT}, 
      #{password,jdbcType=VARCHAR})
  </insert>
  <update id="updateByPrimaryKey" parameterType="com.xgn.cms.domain.User">
    update user
    set userName = #{userName,jdbcType=VARCHAR},
      priority = #{priority,jdbcType=BIGINT},
      password = #{password,jdbcType=VARCHAR}
    where userId = #{userId,jdbcType=INTEGER}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select userId, userName, priority, password
    from user
    where userId = #{userId,jdbcType=INTEGER}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select userId, userName, priority, password
    from user
  </select>
</mapper>